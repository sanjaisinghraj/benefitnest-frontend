{
  "name": "perf-profiles",
  "scripts": {
    "clinic:doctor": "clinic doctor --on-port 'autocannon localhost:3000' -- node ../../backend/index.js",
    "clinic:flame": "clinic flame --on-port 'autocannon localhost:3000' -- node ../../backend/index.js",
    "clinic:heap": "clinic heap-profiler --on-port 'autocannon localhost:3000' -- node ../../backend/index.js"
  },
  "devDependencies": {
    "clinic": "^12.0.0",
    "autocannon": "^7.10.0"
  }
}
